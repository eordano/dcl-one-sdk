[package]
name = "catalyrst-auth-chain"
version = "0.23.0"
edition = "2021"
description = "Decentraland auth-chain wire types and the address / hex / pointer / deep-link primitives shared by dcl-one-sdk and the catalyrst services"

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }
hex = "0.4"

[dev-dependencies]
serde_json = { workspace = true }
